# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['D:\\数据库实验\\project\\src\\main\\GUI.py', 'D:\\数据库实验\\project\\src\\dao\\StudentDao.py', 'D:\\数据库实验\\project\\src\\po\\student.py', 'D:\\数据库实验\\project\\src\\util\\DbUtil.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='GUI',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='GUI',
)
